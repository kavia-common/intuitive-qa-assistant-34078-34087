# Environment variables used by the Android app build/runtime (injected by orchestrator/CI)
# Base URL of the backend API (no trailing slash). For local emulator, 10.0.2.2 points to host.
BACKEND_BASE_URL=http://10.0.2.2:8000
